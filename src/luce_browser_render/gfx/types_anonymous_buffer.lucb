#==============================================================================================
#
#   types_anonymous_buffer - declarations of Libraries/LibCore/AnonymousBuffer.h
#
#   DESCRIPTION:
#       Structs, enums, vtables, class information and cast helpers generated from
#       Libraries/LibCore/AnonymousBuffer.h (Ladybird 47c82b38d0) by the skeleton generator
#       (DESIGN.md §4.4). Regenerate rather than edit, except to fill a field the generator
#       could not map.
#
#       Fixed by hand (r08): the struct was opaque; PaletteImpl keeps its SystemTheme in
#       one, so AnonymousBuffer's `RefPtr<AnonymousBufferImpl>` and the impl's fields
#       (AnonymousBuffer.h lines 19-77) are declared here. anonymous_buffer.lucb ports the
#       members Palette uses.
#
#==============================================================================================

# mark: AnonymousBufferImpl =====================================================================

## Core::AnonymousBufferImpl (Libraries/LibCore/AnonymousBuffer.h:19).
pub struct CoreAnonymousBufferImpl:
    pub var m_fd: i32 = -1
    pub var m_size: usize
    pub var m_data: void*?

# mark: AnonymousBuffer =========================================================================

## Core::AnonymousBuffer (Libraries/LibCore/AnonymousBuffer.h:38). A zero value is the default
## (invalid) buffer.
pub struct CoreAnonymousBuffer:
    pub var m_impl: CoreAnonymousBufferImpl*?
